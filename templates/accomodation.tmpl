{{ define "accommodation" }}

    <div class="flex">
        <!-- Page Title -->
        <h1 class="text-3xl font-bold text-gray-800 mb-8 text-center">Accomodation Options</h1>
    
        <div id="accommodation-cards" class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 py-10 max-w-6xl mx-auto px-4"></div>

    </div>

{{ end }}